const config = {
  printWidth: 80,
  tabWidth: 2,
  bracketSpacing: true,
  bracketSameLine: false,
  singleQuote: true,
  trailingComma: 'all',
  arrowParens: 'always',
  semi: true,
  quoteProps: 'consistent',
};

export default config;
